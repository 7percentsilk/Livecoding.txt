let cpm = 120/2;


samples({ triangle:
'https://raw.githubusercontent.com/shaanrxx/strudelSongs/main/docs/triangleloop.mp3'});
$: sound("triangle").loopAt(3).hurry("<1.5>")

$:sound("num: 1").gain(1.9)


// // // drums
$: sound("bd")
  .euclid(6, 8) // 7 kicks spread over 16 steps
  // .bank("rolandd70")


$: sound("~ sd ~ ~ ~ sd ~ ~").bank("rolandd70") // snare off the beat
$: sound("shaker_large shaker_large shaker_large shaker_large
shaker_large shaker_large shaker_large shaker_large")
  .iter("<2 4 1 2>")
  .gain(0.6)
$: sound("lt(<3 5 2>,8)").bank("rolandd70").legato(0.7).sustain(0.2)
  .hpf(tri.range(4000, 10000))
$: sound("rim").euclid(5,8).bank("rolandd70") // 5 hits over 8 steps —
syncopated!
$: sound("hh!<4 8 2>").bank("rolandd70")

// // //notes
$: sound("ballwhistle ~ ~ ~ ~ ~ ballwhistle ~").legato(1.5)
$: note("[c4 e4] [g4 a4] [g4 d4] [e4]")
  .iter(4)
  .sound("gm_trumpet")
  .clip("0.1 0.2 0.5 0.8")
  .ply("2 1 1 3")
  //.legato(0.6)
  .gain(1.1)
